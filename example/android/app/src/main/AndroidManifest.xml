<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="MapMetrics"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Pins the gateway origin for MMMapSession, which is what lets the
             SDK open a v2 map session and sign tiles with it. Without an origin
             createIfConfigured is a no-op and every tile falls back to v1
             billing: one charge PER TILE on a cold load rather than one per
             session, with no visible symptom.

             PRODUCTION is pinned here because this example is what a customer
             copies. gateway.mapmetrics-atlas.net is also on the SDK's built-in
             allow-list, so an app talking to it can omit this entirely and the
             origin is learned from the first request. It is spelled out because
             an example should show the explicit form.

             To run against staging, replace the value with
             https://gateway-mapatlas-staging.jim9710.workers.dev . Staging is
             NOT on the allow-list, so pinning is the only way to reach it. -->
        <meta-data
            android:name="org.maplibre.android.MapSessionOrigin"
            android:value="https://gateway.mapmetrics-atlas.net" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
</manifest>
